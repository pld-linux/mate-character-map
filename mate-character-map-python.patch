--- mate-character-map-1.6.0/configure.ac.orig	2013-04-02 23:15:04.000000000 +0200
+++ mate-character-map-1.6.0/configure.ac	2013-12-19 15:42:43.123044507 +0100
@@ -229,11 +229,11 @@
   AC_MSG_RESULT([$PYGTK_DEFSDIR])
 
   AC_MSG_CHECKING([for pygtk codegen])
-  PYGTK_CODEGEN="$PYTHON `$PKG_CONFIG --variable=codegendir pygtk-2.0`/codegen.py"
+  PYGTK_CODEGEN="$PYTHON `$PKG_CONFIG --variable=codegendir pygtk-2.0`/codegen.pyc"
   AC_MSG_RESULT([$PYGTK_CODEGEN])
 
   AC_MSG_CHECKING([for pygtk h2def])
-  PYGTK_H2DEF="$PYTHON `$PKG_CONFIG --variable=codegendir pygtk-2.0`/h2def.py"
+  PYGTK_H2DEF="$PYTHON `$PKG_CONFIG --variable=codegendir pygtk-2.0`/h2def.pyc"
   AC_MSG_RESULT([$PYGTK_H2DEF])
 
   AC_SUBST([PYGTK_DEFSDIR])
